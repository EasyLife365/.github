name: Security

on:
  pull_request:
  schedule:
    - cron: '17 3 * * 1'

jobs:
  call-security:
    uses: EasyLife365/.github/.github/workflows/security.yml@main
    secrets:
      package-token: ${{ secrets.PACKAGE_TOKEN }}
